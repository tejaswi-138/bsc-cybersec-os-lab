{
  "name": "CyberSec OS Lab",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo dpkg --add-architecture i386 && sudo apt-get update && sudo apt-get install -y build-essential gcc gdb valgrind mingw-w64 wine wine32:i386",
  "containerEnv": {
    "WINEDEBUG": "-all",
    "WINEARCH": "win64"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cpptools-extension-pack"
      ]
    }
  }
}
